def spin_words(sentence):
    
    words=sentence.split()    
    newwords=[]
    for x in words:
        if len(x)>=5:
            newwords.append(x[::-1])
        else:
            newwords.append(x)
        
    return ' '.join(newwords)
